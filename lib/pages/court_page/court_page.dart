import 'package:flutter/material.dart';


class CourtPage extends StatelessWidget {
  const CourtPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("球馆中心"),
      ),
    );
  }
}
